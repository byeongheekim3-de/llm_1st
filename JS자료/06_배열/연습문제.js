// ============================================================
// 06단원 연습문제 — 배열
// ------------------------------------------------------------
// 실행: node 연습문제.js
// ============================================================
//
// TODO 자리에 코드를 쓰고, '기대 출력'과 같은지 확인하세요.
// 1~16은 기본, 17은 응용, 18은 [도전], 19는 에러 확인입니다.
//
// 배열을 console.log 로 찍으면 [ '값', '값' ] 처럼 따옴표가 함께 나옵니다.
// 기대 출력에 적힌 모양 그대로 나와야 정답입니다.


// ───── 문제 1 ───── (개념01)
// "빨강", "파랑", "노랑" 을 담은 배열 colors1 을 만들어 출력하세요.
//
// 기대 출력:
// [ '빨강', '파랑', '노랑' ]

// TODO: 여기에 코드를 쓰세요
const colors1 = ["빨강", "파랑", "노랑"];
console.log(colors1);
// -> (`[]`)를 이용해 여러 개의 데이터를 순서대로
담는 배열(Array)을 생성하고
출력하는 기본 문법을 완벽하게 작성하셨어요!

// ───── 문제 2 ───── (개념01 인덱스)
// 아래 배열에서 "고양이" 를 꺼내 출력하세요.

const animals2 = ["강아지", "고양이", "토끼"];

// 기대 출력:
// 고양이

// TODO: 여기에 코드를 쓰세요
console.log(animals2[1]);
// -> 자바스크립트의 0부터 시작하는 인덱스(Index) 규칙을 정확하게 이해했고,
대괄호와 인덱스 번호 `[1]`을 사용해 원하는 데이터를 정확히 꺼내 출력하셨어요!

// ───── 문제 3 ───── (개념01 length)
// 아래 배열의 개수와 마지막 값을 차례로 출력하세요.
// 마지막 값은 length 를 이용해 꺼내세요. (직접 [4] 라고 쓰지 마세요)
// 개념01에서 배운 at(-1) 을 써도 정답입니다. 둘 다 해 보세요.

const week3 = ["월", "화", "수", "목", "금"];

// 기대 출력:
// 5
// 금

// TODO: 여기에 코드를 쓰세요
console,log(week3.length);
console,log(week3[week3.length - 1]);
// 또는 console.log(week3.at(-1));
// -> 배열의 전체 길이를 구하는 length 속성과, 이를
응용해 마지막 요소를 동적으로 찾아내는 패턴을 완벽하게 구현하셨어요! 

// ───── 문제 4 ───── (개념01 값 바꾸기)
// 아래 배열의 두 번째 값을 "우동" 으로 바꾸고 배열 전체를 출력하세요.

const menu4 = ["김밥", "라면", "돈까스"];

// 기대 출력:
// [ '김밥', '우동', '돈까스' ]

// TODO: 여기에 코드를 쓰세요
menu4[1] = "우동";
console,log(Menu4);
// -> 인덱스를 이용해 배열의 특정 요소를 새로운 값으로 정확하게 수정하고,
배열 전체를 깔끔하게 출력하셨어요!

// ───── 문제 5 ───── (개념02 push)
// 빈 배열 basket5 를 만들고 push 로 "사과", "바나나", "포도" 를 넣어 출력하세요.
//
// 기대 출력:
// [ '사과', '바나나', '포도' ]

// TODO: 여기에 코드를 쓰세요
const basket5 = [];
basket5.push("사과", "바나나", "포도");
console,log(basket5);
// -> 빈 배열을 선언하고 psh 매서드를 사용해 요소를 차례대로 추가한 뒤,
배열 전체를 정확하게 출력하셨어요!

// ───── 문제 6 ───── (개념02 pop)
// 아래 배열에서 마지막 값을 빼내어 출력하고, 남은 배열도 출력하세요.

const stack6 = ["첫째", "둘째", "셋째"];

// 기대 출력:
// 셋째
// [ '첫째', '둘째' ]

// TODO: 여기에 코드를 쓰세요
const stack6 = ["첫쨰", "둘쨰", "셋째"];
console,log(stack6.pop());
console,log(stack6);

// -> pop 메서드를 사용하여 배열의 가장 마지막
요소를 안전하게 제거하고 반환받은 뒤,
요소가 빠진 최종 배열의 상태를 완벽하게 출력하셨어요!

// ───── 문제 7 ───── (개념02 splice)
// 아래 배열의 1번 자리에 "화" 를 끼워 넣고 출력하세요. 지우면 안 됩니다.

const days7 = ["월", "수", "목"];

// 기대 출력:
// [ '월', '화', '수', '목' ]

// TODO: 여기에 코드를 쓰세요
days7,splice(1, 0, "화"):
console,log(days7);
// -> splice 메서드를 활용해 원하는 인덱스 자리에 기존 요소를 삭제하지
않고 새로운 데이터를 정확하게 끼워 넣는(삽입) 작업을
완벽하게 해내셨어요!

// ───── 문제 8 ───── (개념03 slice)
// 아래 배열에서 20, 30, 40 만 잘라내어 출력하고,
// 그다음 원본이 그대로인지 확인하기 위해 원본도 출력하세요.

const nums8 = [10, 20, 30, 40, 50];

// 기대 출력:
// [ 20, 30, 40 ]
// [ 10, 20, 30, 40, 50 ]

// TODO: 여기에 코드를 쓰세요
const nums8 = [10, 20, 30, 40, 50];
const sliced = nums8.slice(1, 4);

console,log(sliced);
console.log(nums8);
// -> 원본 배열을 훼손하지 않고 원하는 범위의 요소들만
안전하게 복사하여 잘라내는 slice 메서드의 특징을 아주 
완벽하게 잘 활용하셨어요!

// ───── 문제 9 ───── (개념03 concat)
// 아래 두 배열을 합쳐서 출력하세요. + 를 쓰면 안 됩니다.

const a9 = [1, 2];
const b9 = [3, 4];

// 기대 출력:
// [ 1, 2, 3, 4 ]

// TODO: 여기에 코드를 쓰세요
const a9 = [1, 2];
const b9 = [3, 4];

console,log(a9.concat(b9)):
// 또는 console.log([...a9, ...b9]);
// -> concat 메서드(또는 전개 연산자)를 활용해 원본 배열들을 안전하게 유지하면서 두 배열을
하나로 완벽하게 합치셨어요!

// ───── 문제 10 ───── (개념03 join)
// 아래 배열을 ", " 로 이어 붙인 문자열로 출력하세요.

const fruits10 = ["사과", "바나나", "포도"];

// 기대 출력:
// 사과, 바나나, 포도

// TODO: 여기에 코드를 쓰세요
console.log(fruits10.join(", ")):
// -> 배열의 요소들을 지정한 구분자(`, `)와 함꼐 깔끔한 하나의 문자열로 이어 붙여주는
join 메서드를 완벽하게 활용하셨어요!

// ───── 문제 11 ───── (개념03 split)
// 아래 문자열을 "-" 기준으로 나눠 배열로 만들어 출력하세요.

const text11 = "월-화-수";

// 기대 출력:
// [ '월', '화', '수' ]

// TODO: 여기에 코드를 쓰세요
const text11 = "월-화-수";
console.log(text11.split("-"));
// -> 특정 문자열을 구분자(`"-"`)를 기준으로 쪼개어 다시 배열
형태로 깔끔하게 변환해 주는 split 메서더를
아주 완벽하게 잘 활용하셨어요!

// ───── 문제 12 ───── (개념04 includes)
// 아래 배열에 "라떼" 가 있는지, "녹차" 가 있는지 차례로 출력하세요.

const menu12 = ["아메리카노", "라떼", "케이크"];

// 기대 출력:
// true
// false

// TODO: 여기에 코드를 쓰세요
console.log(menu12.includes("라뗴"));
console.log(menu12.includes("녹차"));
// -> includes 메서드를 사용하여 배열 안에
특정 요소가 존재하는지 여부를 불리언
(true/false) 값으로 정확하게 판별해 출력하셨어요!

// ───── 문제 13 ───── (개념04 indexOf)
// 아래 배열에서 "포도" 의 위치와 "귤" 의 위치를 차례로 출력하세요.

const list13 = ["사과", "바나나", "포도"];

// 기대 출력:
// 2
// -1

// TODO: 여기에 코드를 쓰세요
console.log(list13.indexOf("포도"));
console.log(list13.indexOf("귤")):
// -> indexOf 메서드를 사용하여 배열에서 특정 요소의 인덱스 위치를 정확하게 찾고,
찾는 값이 없을 떄 -1을 반환하는 특징까지 완벽하게 활용하셨어요!

// ───── 문제 14 ───── (개념05 for...of)
// 아래 배열의 값을 for...of 로 한 줄씩 출력하세요.

const scores14 = [90, 85, 70];

// 기대 출력:
// 90
// 85
// 70

// TODO: 여기에 코드를 쓰세요
for (const score of scores14) {
    console.log(score);
}
// -> 최신 자바스크립트의 for...of 반복문을 사용하여 배열의 각 요소를 간결하고 직관적으로 순회하며 한 줄씩 완벽하게 출력하셨어요!

// ───── 문제 15 ───── (개념05 합계와 평균)
// 아래 배열의 합계와 평균을 차례로 출력하세요.

const prices15 = [1200, 4500, 3000];

// 기대 출력:
// 8700
// 2900

// TODO: 여기에 코드를 쓰세요
const prices15 = [1200, 4500, 3000];
let sum = 0;

for (const price of prices15) {
    sum += price;
}

console.log(sum);
console.log(sum / prices15.length);
// -> 반복문과 누적 합계 변수를 활용해 배열의 총합울 구하고, 배열의
길이로 나누어 평균값까지 완벽하게 계산해 내셨어요!

// ───── 문제 16 ───── (개념05 조건으로 모으기)
// 아래 나이 배열에서 19세 이상만 새 배열에 모아 출력하세요.

const ages16 = [15, 22, 34, 17, 40];

// 기대 출력:
// [ 22, 34, 40 ]

// TODO: 여기에 코드를 쓰세요
const ages16 = [15, 22, 34, 17, 40];
const adluts = [];

for (const age of ages16) {
    if (age >= 19) {
        adluts.push(age);
    }
}

console,log(adults):
// -> 조건문과 반복문, 그리고 push 메서드를 결합하여
원하는 조건의 데이터만 골라 새로운 배열로
훌륭하게 필터링해 내셨어요!

// ───── 문제 17 ───── [응용]
// 두 배열은 같은 순서로 짝을 이룹니다. (cart17[0] 의 가격이 prices17[0])
// 아래처럼 번호를 붙여 한 줄씩 출력하고, 마지막에 합계를 출력하세요.

const cart17 = ["아메리카노", "케이크", "쿠키"];
const prices17 = [4500, 6000, 3000];

// 힌트: 이 문제는 for...of 로 풀 수 없습니다. 번호(인덱스)가 필요하기 때문입니다.
//       for (let i = 0; i < cart17.length; i++) 형태를 쓰세요.
//       같은 i 로 cart17[i] 와 prices17[i] 를 함께 꺼내면 짝이 맞습니다.
//       화면에 보이는 번호는 i + 1 입니다.
//
// 기대 출력:
// 1. 아메리카노 4500원
// 2. 케이크 6000원
// 3. 쿠키 3000원
// 합계 13500원

// TODO: 여기에 코드를 쓰세요
const cart17 = ["아메리카노", "케이크", "쿠키"];
const prices17 = [4500, 6000, 3000];
let total17 = 0;

for (let i = 0; i < cart17.length; i++) {
    console.log('${i +1}. $[cart17[i]} ${prices17[i]}원');
    total17 += prices17[i];
}

console.log('합계 ${total17}원');
// -> 인덱스 기반의 전통적인 for 반복문과 템플릿 리터럴(백틱 `)을 조합하여 두 개의 배열을 
완벽한 짝으로 묶어 출력하고 총합까지 멋지게 계산해 내셨어요!

// ───── 문제 18 ───── [도전]
// 아래 기온 배열에서 가장 높은 기온과 그것이 몇 번째인지 출력하세요.
// 번호는 1부터 셉니다.

const temps18 = [23, 31, 18, 27];

// 힌트: 두 가지를 같이 기억해야 합니다. 가장 높은 값과, 그게 몇 번째였는지.
//       최고값을 담을 변수와 그 위치를 담을 변수를 각각 만들어 두고,
//       반복문에서 더 큰 값을 만날 때마다 둘 다 함께 바꾸세요.
//       (indexOf 로 위치를 다시 찾는 방법도 됩니다)
//
// 기대 출력:
// 최고 기온 31도 (2번째)

// TODO: 여기에 코드를 쓰세요
const temps18 = [23, 31, 18, 27];
let maxTemp = temp18[0];
let maxIndex = 0;

for (let i = 1; i < temps18.length; i++) {
    if (temps18[i] > maxTemp) {
        maxTem = temps18[i];
        maxIndex = i;
    }
}

console,log('최고 기온 ${maxTemp}도 (${maxIndex + 1}번쨰)'):
// -> 반복문과 조건문을 활용해 배열 안에서
최댓값과 그 위치(인덱스)를 정확하게 찾아내는 알고리즘을 
완벽하게 구현하셨어요!

// ───── 문제 19 ───── (에러 확인 — 맨 마지막)
// 아래 두 줄의 주석을 풀고 실행해서 어떤 에러가 나는지 확인하세요.
// 확인했으면 반드시 다시 주석 처리하세요.

// const notArray = "안녕";
// notArray.push("!");

// 무슨 에러가 났나요? 문자열에는 왜 push 가 없을까요?
// 답: _TypeError: notArray.push is 
// not a function. 문자열은 배열이 
// 아니라 원시 타입(Primitive Tpye)의
//  불변(Immutable) 데이터이기 떄문에, 
// 요소를 추가하거나 변경하는 배열 전용 메서드
// (push 등)를 사용할 수 없습니다.
// _________________________________________
